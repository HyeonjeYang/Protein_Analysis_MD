# Wizard

`pamd wizard` asks a short set of questions and writes a concise YAML config.
It does not run MD. Compile the generated config before preparation:

```bash
pamd wizard
pamd compile configs/my_project.yaml
```
