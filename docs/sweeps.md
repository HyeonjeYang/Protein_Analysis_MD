# Sweeps

The concise config format accepts a `sweep` block for pH, ionic strength, PTM
state, cleavage state, and replicate dimensions. The current compiler records
sweep definitions in `config_resolved.json` and lock metadata for deterministic
expansion in later design stages.
