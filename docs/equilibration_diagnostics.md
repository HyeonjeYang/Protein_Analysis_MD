# Equilibration Diagnostics

Analysis writes heuristic equilibration diagnostics when Rg or energy time
series are available. Outputs include `equilibration_diagnostics.json` and
`equilibration_blocks.parquet`.

These diagnostics are suggestions for inspection, not proof of equilibrium.
